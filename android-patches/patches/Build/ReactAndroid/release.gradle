--- ./ReactAndroid/release.gradle	2021-10-21 22:26:33.000000000 -0700
+++ /var/folders/vs/8_b205053dddbcv7btj0w0v80000gn/T/update-Ge4Sm3/merge/Build/ReactAndroid/release.gradle	2021-10-25 12:26:02.000000000 -0700
@@ -74,6 +74,7 @@
 afterEvaluate { project ->
 
     task androidJavadoc(type: Javadoc, dependsOn: generateReleaseBuildConfig) {
+        failOnError false
         source = android.sourceSets.main.java.srcDirs
         classpath += files(android.bootClasspath)
         classpath += files(project.getConfigurations().getByName("compile").asList())
@@ -130,7 +131,9 @@ afterEvaluate { project ->
         }
     }
 
-    task installArchives(type: Upload) {
+    tasks.mergeReleaseJniLibFolders.dependsOn packageReactNdkLibs
+
+    task installArchives(type: Upload,dependsOn: mergeReleaseJniLibFolders) {
         configuration = configurations.archives
         repositories.mavenDeployer {
             // Deploy to react-native/android, ready to publish to npm
